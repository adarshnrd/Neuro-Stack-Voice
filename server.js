// DEPRECATED — the real entry point is src/server.ts, built to
// dist/server.js via `npm run build` and started with `npm start`.
// This root-level server.js is kept only because the tooling used for this
// restructure could not delete files, only edit their content. Safe to
// delete:
//
//   git rm server.js
